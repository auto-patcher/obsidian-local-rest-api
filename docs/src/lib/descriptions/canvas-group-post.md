# Create Canvas Group

Create a new group node in a canvas. The group ID is generated automatically.

Request body should contain group properties: x, y, width, height, and optional properties like label, color, background, backgroundStyle, etc.

