# Create Canvas Node

Create a new node in a canvas. The node ID is generated automatically.

Request body should contain node properties: type, x, y, width, height, and optional properties like text, file, url, color, etc.

