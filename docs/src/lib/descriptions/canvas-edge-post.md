# Create Canvas Edge

Create a new edge (connection) between nodes in a canvas. The edge ID is generated automatically.

Request body should contain edge properties: fromNode, toNode, and optional properties like fromSide, toSide, fromEnd, toEnd, color, label, etc.

