# Get Canvas Group

Get a group node and all nodes spatially contained within it.

Returns an object with:
- `group`: The group node object
- `containedNodes`: Array of nodes whose bounding boxes are fully contained within the group's bounding box

